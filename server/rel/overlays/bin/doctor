#!/bin/sh
# Diagnoses the installation against omashiki.toml.
# The release form of `mix omashiki.doctor`: same checks, same output.
# Exits non-zero when any check is an error.
set -e

cd "$(dirname "$0")/.."

exec ./bin/omashiki eval 'Omashiki.Cli.run(Omashiki.Cli.Doctor, System.argv())' "$@"
