# Distributed VM harness

This directory contains the VM preparation and distributed execution harness.
`manifest.toml` defines the pinned artifacts, topology, resources, and runtime settings.
Use [the distributed test procedure](../docs/internal/how-to-run-distributed-tests.md) for commands and cleanup behavior.
Use [the Kata reference](../docs/internal/runtime-kata.md) for runtime compatibility checks.
